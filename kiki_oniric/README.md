# kiki_oniric

Dedicated fork of `kiki-flow-core` for the dreamOfkiki research
program (Track A).

**Status** : Skeleton S1. Fork content will be populated S2 (Story 0
expose-primitives).

**Source** : `~/Documents/Projets/kiki-flow-research/` (kiki-flow-core
parent), main HEAD at time of fork.

**Rebase policy** (r3 jalonné, see docs/fork-decision.md) :
- S1 fork (now)
- S8 mid-rebase
- S18 pre-paper rebase

**DO NOT** modify files in this directory directly outside of planned
tasks (Story 0 S2.2 for expose-primitives, then P_min/P_equ/P_max
implementations per plan Phase 2).

## Package identifier

Python imports use `kiki_oniric` (underscore). See
`docs/glossary.md` for canonical naming and `docs/fork-decision.md`
for rationale.
